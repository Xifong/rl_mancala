[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mancala_env"]

[project]
name = "mancala_env"
version = "0.0.1"
dependencies = [
    "gymnasium",
    "pygame==2.6.1",
]
requires-python = ">= 3.12.4"
